# オブジェクト

## オブジェクトとは

値に名前をつけて管理する。
```js:main.js
{
  // const point = [100, 180]; 

  // const point = {x: 100, y:180}; 

  const point = {
    x: 100, 
    y: 180, 
  }; 
  console.log(point); 
}
```
>{x: 100, y: 180}

オブジェクトを構成する要素ひとつひとつを __プロパティ(メンバー）__
左側を __名前（キー）__ 右側を __値__ と呼ぶ
